---
import Layout from '~/layouts/Layout.astro';
import { site } from '~/content/site';
---

<Layout title={`${site.name.en} — ${site.tagline.en}`} description={site.description.en}>
  <main id="main">
    {/* The team will fill in: Nav, Hero with Ignition, Manifesto, Work, Contact, Footer. */}
    <section class="container-page py-32">
      <p class="text-lead text-ink-soft">Scaffold ready. Hero ignition coming next.</p>
    </section>
  </main>
</Layout>
